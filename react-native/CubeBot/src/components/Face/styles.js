import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        height: 135,
        width: 135,
        borderColor: 'black',
        borderWidth: 1
    },
    linha: {
        flexDirection: 'row',
    }
});

export default styles;